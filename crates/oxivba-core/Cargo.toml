[package]
name = "oxivba-core"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
description = "VBA source analysis: lexer, parser, and static diagnostics"

# Zero dependencies, for the same reasons as oxicells-calc: this has to stay
# usable from wasm32 and from a CLI, and it holds no I/O and no host types.
[dependencies]
