Self-authored keep-next fixtures. CC BY-SA 4.0.

Word measurements in word.json cover fixed and automatic line spacing,
inherited heading styles, widow control, and the last column of a page.
The regression test checks page counts, heading positions, and the first
body line's page. DOCX hashes in word.json identify the measured inputs.
