# Changelog

## 0.8.0

First release. Opens `.docx`, `.xlsx` and `.pptx` in a VS Code tab, rendered by
Oxi 0.8.0 — the same WebAssembly build that powers the browser demo.

Read-only: the document is rendered, not saved back.
